#include <stdio.h>
#include <string.h>

int main() {
    char password[] = "password123";
    FILE *fptr;

    fptr = fopen("101-password", "w");
    fprintf(fptr, "%s", password);
    fclose(fptr);

    return 0;
}
